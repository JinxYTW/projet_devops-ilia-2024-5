import { MessageController } from '../js/controller/message-controller.js';

document.addEventListener('DOMContentLoaded', () => {
    const controller = new MessageController();
    controller.init();
});
